const PRODUCT = '/product';
const PRODUCTS = '/products';
const PRODUCT_DETAIL = `${PRODUCT}/:productId`;
const CMS = `/cms`;
const LIST_PRODUCT = `${CMS}/list-product`;
const ADD_PRODUCT = `${CMS}/add-product`;

const PATH_URL = {
    PRODUCT,
    PRODUCTS,
    PRODUCT_DETAIL,
    LIST_PRODUCT,
    ADD_PRODUCT
}

export default PATH_URL;